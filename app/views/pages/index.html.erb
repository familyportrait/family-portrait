<section>
	<div class="container">
		<div class="building-wrap">
			<%= image_tag "index/house.png", :alt => "house", :class => "house" %>
		</div>	
	</div>
</section>

<section class="char">
	<div class="container">
		<% @users.each do |user|%>
		<ul>
				<li class="user"><%= user.email %></li>
		</ul>
		<% end %>
	</div>	
</section>	



<script>
function randomIntFromInterval(min,max)
{
    return Math.floor(Math.random()*(max-min+1)+min);
}

  $('ul li.user').on('click', function() {
  console.log("hi");
   for(var i = 0; i < 3; i++){
     var x = randomIntFromInterval(-250,250);
     var y = randomIntFromInterval(0,200);
     var rotate = randomIntFromInterval(-180,180); 
     var card = $('.card').eq(i); 
      
      // $(this).css({top: y + 'px', right: x + 'px' , transform: 'rotate(' + rotate + 'deg)' });
      $(this).css({transform: 'translate(' + y + 'px,' + x + 'px)' });
      }
});

$('ul li.user').hover(function() {
  $(this).toggleClass('show');

});
</script>